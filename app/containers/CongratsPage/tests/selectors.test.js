// import { selectCongratsPageDomain } from '../selectors';

describe('selectCongratsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
