import styled from 'styled-components';

import NormalImg from 'components/Img';

const Img = styled(NormalImg)`
  max-width: 120px;
  margin: 0 auto;
  display: block;
  max-height: 70px;
`;

export default Img;
