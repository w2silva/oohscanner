/*
 * CampaignListFilter Messages
 *
 * This contains all the text for the CampaignListFilter component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CampaignListFilter';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CampaignListFilter component!',
  },
});
