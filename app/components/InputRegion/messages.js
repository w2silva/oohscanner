/*
 * InputRegion Messages
 *
 * This contains all the text for the InputRegion component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.InputRegion';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InputRegion component!',
  },
});
